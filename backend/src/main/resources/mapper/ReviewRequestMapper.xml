<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.valueguard.mapper.ReviewRequestMapper">
    
    <resultMap id="ReviewRequestResultMap" type="ReviewRequest">
        <id property="id" column="id"/>
        <result property="incidentId" column="incident_id"/>
        <result property="review" column="review"/>
        <result property="status" column="status" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <select id="findById" resultMap="ReviewRequestResultMap">
        SELECT * FROM review_requests WHERE id = #{id}
    </select>
    
    <select id="findByIncidentId" resultMap="ReviewRequestResultMap">
        SELECT * FROM review_requests WHERE incident_id = #{incidentId}
        ORDER BY created_at DESC
    </select>
    
    <insert id="insert" parameterType="ReviewRequest" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO review_requests (id, incident_id, review, status, created_at, updated_at)
        VALUES (#{id}, #{incidentId}, #{review}, 
                COALESCE(#{status, typeHandler=org.apache.ibatis.type.EnumTypeHandler}, 'PENDING'), 
                NOW(), NOW())
    </insert>
    
    <update id="update" parameterType="ReviewRequest">
        UPDATE review_requests
        SET incident_id = #{incidentId},
            review = #{review},
            status = #{status, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
            updated_at = NOW()
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM review_requests WHERE id = #{id}
    </delete>
    
    <select id="findAll" resultMap="ReviewRequestResultMap">
        SELECT * FROM review_requests ORDER BY created_at DESC
    </select>
</mapper>

